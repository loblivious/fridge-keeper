services:
  postgres:
    image: postgres:18.1-alpine
    environment:
      POSTGRES_DB: fridgekeeper
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
